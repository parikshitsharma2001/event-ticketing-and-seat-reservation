apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: seatingdb-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: seatingdb-config
data:
  POSTGRES_DB: seatingdb
  POSTGRES_USER: postgres
---
apiVersion: v1
kind: Secret
metadata:
  name: seatingdb-secret
type: Opaque
stringData:
  POSTGRES_PASSWORD: postgres
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: seatingdb
  labels:
    app: seatingdb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: seatingdb
  template:
    metadata:
      labels:
        app: seatingdb
    spec:
      containers:
      - name: postgres
        image: postgres:14-alpine
        ports:
        - containerPort: 5432
        envFrom:
        - configMapRef:
            name: seatingdb-config
        - secretRef:
            name: seatingdb-secret
        volumeMounts:
        - name: seatingdb-storage
          mountPath: /var/lib/postgresql/data
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - postgres
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - postgres
          initialDelaySeconds: 5
          periodSeconds: 5
      volumes:
      - name: seatingdb-storage
        persistentVolumeClaim:
          claimName: seatingdb-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: seatingdb
  labels:
    app: seatingdb
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
  selector:
    app: seatingdb
